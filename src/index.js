import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

let name = <p>farras</p>;

ReactDOM.render(<App />, document.getElementById("root"));

ReactDOM.render(name, document.getElementById("cont"));
